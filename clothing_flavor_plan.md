# خطة تنفيذ Flavor محلات الملابس — ERP_App

مرجع شامل لتنفيذ ودعم نشاط محلات الملابس والأحذية (Clothing & Fashion Flavor).
يتم التنفيذ مرحلة بمرحلة، وفقاً لقواعد المشروع في `AGENTS.md`.

---

## Phase 0 — البنية التحتية والنسخة المجانية (Infra & Free Version) ✅ مكتمل

**الهدف:** فصل تطبيق محلات الملابس كـ flavor مستقل مع تفعيل وضع النسخة المجانية (بدون قيود ترخيص).

- [x] **نقطة الدخول:** إنشاء `lib/main_clothing.dart` مع تمرير `Flavor.clothing`.
- [x] **تكوين الـ Flavor:** تعريف `Flavor.clothing` في `lib/core/config/app_flavor.dart`:
  - اسم قاعدة البيانات: `pos_clothing`
  - عنوان التطبيق: `POS - ملابس`
- [x] **رايات الميزات (Feature Flags):** إضافة `AppFeatures.isClothing` وتعطيل ميزات سوق الخضار (البرنيكة، تتبع الشحنات، عمولة المزارع).
- [x] **النسخة المجانية (Free Version):** تحديث `LicenseConfig.isFreeVersion` ليعمل تلقائياً وبشكل دائم مع `Flavor.clothing` بدون شاشات تفعيل أو أقفال ترخيص.
- [x] **سكربت البناء والإصدار:** تحديث `scripts/build_release.ps1` لدعم معامل `-Flavor clothing` وتفعيل وضع النسخة المجانية آلياً.
- [x] **الاختبارات الآلية:** تغطية واختبار جميع خصائص الـ Flavor وتخطي الترخيص بنجاح في `test/unit/app_flavor_test.dart`.

---

## Phase 1 — المقاسات والألوان والباركود (Product Variants & Barcodes) ✅ مكتمل

**الهدف:** دعم إدارة منتجات الملابس ذات المقاسات والألوان المتعددة مع باركود خاص بكل صنف/مقاس.

- [x] **تحديث جدول الأصناف (Schema v57/v58):** إضافة حقول `size` و `color` القابلة للقيمة الخالية بأمان تام مع دعم التحديثات والـ migrations.
- [x] **تصنيفات وقوائم الملابس:** إنشاء `ClothingConstants` (رجالي، حريمي، أطفال، أحذية، إكسسوارات) وقوالب مقاسات الملابس والأحذية والألوان الشائعة.
- [x] **توليد المتغيرات دفعة واحدة (Variant Generator Dialog):** إمكانية إنشاء مصفوفة المقاسات والألوان لصنف معين بنقرة واحدة وتوليد باركود فريد لكل مقاس/لون تلقائياً.
- [x] **تخصيص نموذج الصنف (Product Form):** إظهار منتقي المقاسات والألوان في حالة `isClothing` وإخفاء تعقب البرنيكة نهائياً.
- [x] **طباعة ملصقات الباركود والأسعار:** تخصيص بطاقات الملصقات لتوضيح المقاس واللون والسعر.
- [x] **الاختبارات الآلية:** اختبارات شاملة لجميع ثوابت الملابس وقاعدة البيانات واختبارات الـ Flavor وجميعها نجحت (14/14).

---

## Phase 2 — شاشات البيع ونقاط الكاشير (POS & Sales Flow)

**الهدف:** سرعة إدخال وبحث المنتجات عند البيع في بيئة محلات الملابس.

- [ ] البحث السريع بالباركود للمقاس/اللون المباشر.
- [ ] واجهة كاشير سهلة ومباشرة خالية من حقول الخضار (بدون وزن أو برنيكة).
- [ ] دعم فواتير الاستبدال والاسترجاع بسهولة وسياسات إرجاع الملابس.

---

## Phase 3 — التقارير والمخزون (Reports & Inventory)

- [ ] تقرير جرد المخزون حسب التصنيف والمقاس.
- [ ] تقرير الأصناف الأكثر مبيعاً والأكثر ربحية في الملابس.
